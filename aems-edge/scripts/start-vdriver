#!/usr/bin/env bash
# Start the VOLTTRON platform driver agent standalone.
#
# Usage:
#   ./start-vdriver
#
# Environment variables (override defaults):
#   VOLTTRON_HOME       - path to volttron home directory
#   VENV_PATH           - path to the Python virtual environment
#   PLATFORM_DRIVER_SRC - path to the platform driver source
#   HOST                - host for the platform (default: localhost)
#   PORT                - port for the platform (default: 8000)

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
EDGE_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

VOLTTRON_HOME="${VOLTTRON_HOME:-$EDGE_DIR/Manager/volttron_home}"
VENV_PATH="${VENV_PATH:-$EDGE_DIR/.venv-edge}"
PLATFORM_DRIVER_SRC="${PLATFORM_DRIVER_SRC:-$HOME/volttron_new/volttron-platform-driver/src}"
HOST="${HOST:-localhost}"
PORT="${PORT:-8000}"

export VOLTTRON_HOME
export GEVENT_SUPPORT=True

# Activate the virtual environment
source "$VENV_PATH/bin/activate"

# Create logs directory
mkdir -p "$EDGE_DIR/logs"

echo "Starting platform driver from: $PLATFORM_DRIVER_SRC"
cd "$PLATFORM_DRIVER_SRC"
python -m platform_driver.agent \
    --host "$HOST" \
    --port "$PORT" \
    --identity platform.driver \
    > "$EDGE_DIR/logs/driver.log" 2>&1 &
disown
echo "  PID: $! -> logs/driver.log"

cd "$EDGE_DIR"
echo "Done. Driver started in background."
