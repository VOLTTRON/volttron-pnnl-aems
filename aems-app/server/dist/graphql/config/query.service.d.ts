import { SchemaBuilderService } from "../builder.service";
import { AppConfigService } from "@/app.config";
export declare class ConfigQuery {
    constructor(builder: SchemaBuilderService, configService: AppConfigService);
}
