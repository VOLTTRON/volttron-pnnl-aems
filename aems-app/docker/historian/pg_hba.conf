# PostgreSQL Client Authentication Configuration File
# This file controls which hosts are allowed to connect and how they authenticate

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections for PostgreSQL superuser
local   all             postgres                                peer

# Local connections for historian user
local   all             historian                               scram-sha-256

# IPv4 local connections
host    all             historian       127.0.0.1/32            scram-sha-256
host    all             historian       ::1/128                 scram-sha-256

# Docker network connections - historian user (ONLY from Docker internal network)
host    historian       historian       172.16.0.0/12           scram-sha-256

# SECURITY: Explicitly deny historian user from external networks
host    all             historian       0.0.0.0/0               reject
host    all             historian       ::/0                    reject

# Replication connections from Docker network - REQUIRE SSL
# Using 'hostssl' because PostgreSQL handles its own SSL with Traefik TLS passthrough
hostssl replication     replicator      172.16.0.0/12           scram-sha-256

# Replication connections through Traefik proxy - REQUIRE SSL
# Traefik uses TLS passthrough, PostgreSQL handles SSL/TLS encryption
# This is secure because: PostgreSQL requires SSL, only exposed through port 6543, requires valid credentials
hostssl replication     replicator      0.0.0.0/0               scram-sha-256

# Allow replicator to connect to historian database through Traefik for monitoring/testing
# This allows tools like pgAdmin to connect and verify the setup
hostssl historian       replicator      0.0.0.0/0               scram-sha-256

# SECURITY: No explicit deny needed for replicator - the final "deny all" rules will catch anything not explicitly allowed above

# Deny all other connections
host    all             all             0.0.0.0/0               reject
host    all             all             ::/0                    reject
