query ReadCurrent {
  readCurrent {
    id
    name
    email
    role
    preferences
    createdAt
    updatedAt
  }
}

mutation UpdateCurrent($update: UserUpdateInput) {
  updateCurrent(update: $update) {
    id
    name
    email
    role
    preferences
    createdAt
    updatedAt
  }
}
