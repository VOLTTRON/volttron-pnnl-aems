import dotenv from "dotenv-flow";

// Load environment variables from .env files
// Import this file for any script that needs access to environment variables and isn't running within Nextjs' environment.

dotenv.config({
  silent: true,
});
